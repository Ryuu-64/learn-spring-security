<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.ryuu.learn.springsecurity.mapper.UserMapper">
    <select id="selectAll" resultType="org.ryuu.learn.springsecurity.model.User">
        SELECT *
        FROM users;
    </select>
    <select id="selectOne" resultType="org.ryuu.learn.springsecurity.model.User">
        SELECT *
        FROM users
        WHERE username = #{username};
    </select>
    <insert id="insert">
        INSERT INTO users (username, password, enabled)
        VALUES (#{username}, #{password}, #{enabled});
    </insert>
</mapper>